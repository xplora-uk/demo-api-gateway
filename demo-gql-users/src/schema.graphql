type Query {
  me: User!
}

type User @key(fields: "id") {
  id: ID!
  username: String!
  # other attributes
}

extend type Campaign @key(fields: "id") {
  id: ID! @external
  owner: User! # <-- injecting the User type
}
